# 手順書（Aさん・Bさん・Cさん向け）

ブラウザだけでできます。インストールは不要です。1人10分くらいです。

## 1. 自分のイシューを確認する
1. リポジトリの「Issues」タブを開く
2. 自分が担当のイシューを開いて、やることを読む

## 2. ブランチを作る
1. 「Code」タブを開く
2. 左上の「main」と書いてあるボタンを押す
3. 名前を入れる（例：`issue-4-add-exercise`）
4. 「Create branch: ○○ from main」を押す

## 3. ファイルを直す
1. 直すファイル（例：`data.js`）を開く
2. 右上のえんぴつマーク（Edit）を押す
3. イシューに書いてあるとおりに直す
4. 右上の「Commit changes...」を押す
5. メッセージを書く（例：`運動を2つ追加 #4`）
   - `#4` のようにイシュー番号を入れると、イシューとつながります
6. 「Commit directly to the issue-4-... branch」になっているのを確認して「Commit changes」

## 4. プルリクエストを出す
1. 画面上に出る「Compare & pull request」を押す
2. 本文に `closes #4`（自分のイシュー番号）と書く
   - マージされるとイシューが自動で閉じます
3. 右側の「Reviewers」でさっちゃんを選ぶ
4. 「Create pull request」を押す

## 5. レビューを待つ
- さっちゃんがコメントしたら、同じブランチでファイルを直せば自動で追加されます
- マージされたら完了です
